<div class="container">
	<h1><%= @question.title %></h1>

		<p style="color:blue">
			<%= @question.content %>
		</p>

	<ul id="#question-comments">
		<% @question.comments.each do |comment| %>
			<li>
				<form method="post" action='/votes/new' class="thumbs_button">
        	<input type="hidden" name="comment_id" value="<%= comment.id %>">
        	<button type="submit" name="value" value="up_vote" class="thumbs_button">&#128077</button>
      	</form>
      	
      	<span class="vote_count"><%= Vote.vote_count(comment.votes) %></span>

	      <form method="post" action='/votes/new' class="thumbs_button">
	        <input type="hidden" name="comment_id" value="<%= comment.id %>">
	        <button type="submit" name="value" value="down_vote" class="thumbs_button">&#128078</button>
	      </form>	
			<%= comment.content %></li>
		<% end %><br>
		<% if @question_errors %>
			<% @question_errors.each do |error| %>
				<%= error %>
			<% end %>
		<% end %>
	</ul>
		<form action='/questions/<%= @question.id %>/comments/new'method="post">
			<label for="comment">Make a comment: </label>
			<input type="text" name="content">
			<input type="submit" value="Comment"><br>
		</form>

<hr>
<h2>Answers:</h2>

			<% @question.answers.each do |answer| %>
		<ul>
				<li style="color:red">
					<form method="post" action='/votes/new' class="thumbs_button">
	        	<input type="hidden" name="answer_id" value="<%= answer.id %>">
	        	<button type="submit" name="value" value="up_vote" class="thumbs_button">&#128077</button>
	      	</form>
	      	
	      	<span class="vote_count"><%= Vote.vote_count(answer.votes) %></span>

		      <form method="post" action='/votes/new' class="thumbs_button">
		        <input type="hidden" name="answer_id" value="<%= answer.id %>">
		        <button type="submit" name="value" value="down_vote" class="thumbs_button">&#128078</button>
		      </form>

				<%= answer.content %></li>
				<p style="color:green">Comments:</p>

					<ul>
						<% answer.comments.each do |comment| %>
							<li>
								<form method="post" action='/votes/new' class="thumbs_button">
				        	<input type="hidden" name="comment_id" value="<%= comment.id %>">
				        	<button type="submit" name="value" value="up_vote" class="thumbs_button">&#128077</button>
				      	</form>
				      	
				      	<span class="vote_count"><%= Vote.vote_count(comment.votes) %></span>

					      <form method="post" action='/votes/new' class="thumbs_button">
					        <input type="hidden" name="comment_id" value="<%= comment.id %>">
					        <button type="submit" name="value" value="down_vote" class="thumbs_button">&#128078</button>
					      </form>
							<%= comment.content %></li>
						<% end %>
					</ul><br>
		</ul>
		<% if @answer_errors && @bad_answer_id == answer.id %>
				<% @answer_errors.each do |error| %>
				<%= error %>
			<% end %>
		<% end %>
		<form action='/questions/<%= @question.id %>/answer/<%= answer.id %>/new' method="post">
			<label for="comment">Make a comment: </label>
			<input type="text" name="content">
			<input type="submit" value="Comment"><br>
		</form>
			<% end %>


</div>


<!-- Reminder, turn vote count into method in model -->
